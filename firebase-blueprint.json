{
  "entities": {
    "Message": {
      "title": "Message",
      "description": "A single message in the chat history.",
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "The text content of the message." },
        "role": { "type": "string", "enum": ["user", "model"], "description": "Who sent the message." },
        "timestamp": { "type": "string", "format": "date-time", "description": "When the message was sent." },
        "userId": { "type": "string", "description": "The ID of the user who owns this history." }
      },
      "required": ["text", "role", "timestamp", "userId"]
    }
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": "Message",
      "description": "Collection of chat messages for persistence and memory."
    }
  }
}
